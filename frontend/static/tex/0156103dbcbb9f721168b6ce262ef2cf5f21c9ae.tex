\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec, pgfplots}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Tags: absolute value rules;properties of equality, equation rules;proper subset;equivalence properties of equality;element of a set
:Prob: 0.7967;0.7953;0.6785;0.667;0.6098
:Author: Prof.Hu Ji-shan, HKUST
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering 

\pgfplotsset{
    colormap/outside/.style={
        colormap=
            {outside}{
            rgb255(0cm)=(110,110,255);
            rgb255(1cm)=(20,20,255);
            }
    },
    colormap/outside,
    colormap/inside/.style={
        colormap={inside}{
            rgb255(0cm)=(20,20,255);
            rgb255(1cm)=(220,220,255);
        }
    },
    colormap/inside
}

\begin{tikzpicture}[font=\footnotesize]
\pgfplotsset{compat=1.8}
\begin{axis}
[axis lines = center, view={120}{10}, scale=1.2, ticks=none,
axis background, xlabel = {$x$}, ylabel ={$y$}, zlabel ={$z$}, domain =-2:2, y domain =-2:2,
samples =20, samples y =40, z buffer = auto, 
axis equal, 
%xmin=-4, xmax=4,
%ymin=-4, ymax=4,
%zmin=0, zmax=1.2,
every axis x label/.style={
    at={(ticklabel* cs:1)},
    anchor= east, xshift = 2, yshift=-4
},
every axis y label/.style={
    at={(ticklabel* cs:1)},
    anchor= west, 
},
every axis z label/.style={
    at={(ticklabel* cs:1)},
    anchor= south
}]


    \addplot3 [
      surf, 
      domain=0:4,
      samples=2,
      y domain=-3:4,
      samples y=2,
%      line join=round,  
      shader=interp,
%      mesh/interior colormap name=inside,
%      colormap/outside,
      shader=faceted,
      variable=\t,
%      point meta={t},
      faceted color=black, opacity=0.8
    ]
		({t},{y},{(11*t+8*y-17)/6});

    \addplot3 [
      surf, red, 
      domain=0:1,
      samples=2,
      y domain=-4:4,
      samples y=2,
      line join=round, smooth, 
      mesh/interior colormap name=inside,
      colormap/outside,
      shader=faceted,
      variable=\t,
      point meta={t},
      faceted color=black, opacity=1
    ]
		({1+(2)*t},{0+(-2)*t},{-1+(1)*t});

    \addplot3 [
      surf, red, 
      domain=0:1,
      samples=2,
      y domain=-4:4,
      samples y=2,
      line join=round, smooth, 
      mesh/interior colormap name=inside,
      colormap/outside,
      shader=faceted,
      variable=\t,
      point meta={t},
      faceted color=black, opacity=1
    ]
		({1+0*t},{0+(3)*t},{-1+(4)*t});

    \addplot3 [
      surf, red, 
      domain=0:1,
      samples=2,
      y domain=-4:4,
      samples y=2,
      line join=round, smooth, 
      mesh/interior colormap name=inside,
      colormap/outside,
      shader=faceted,
      variable=\t,
      point meta={t},
      faceted color=black, opacity=1
    ]
		({1+2*t},{3+(-5)*t},{3+(-3)*t});

\node[label={-90:{$A$}}] at (axis cs:1,0,-0.6) {};
\node[label={90:{$B$}}] at (axis cs:3,-2,-0.2) {};
\node[label={45:{$C$}}] at (axis cs:1.2,2.5,2.5) {};

\end{axis}

\end{tikzpicture}
\end{document}